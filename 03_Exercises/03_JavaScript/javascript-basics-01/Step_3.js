var name="username";
//there are three functions to open a dialog boxes (pop-up ) [Alert : just warning with Ok button],[Confirm: question with ok or cancel button (yes/no)],
//[prompt: question with a field to fill with yes/no and it return the answer(so you need to assign it to a variable)]
name = prompt('Please enter your name');
alert('Hello '+name);

